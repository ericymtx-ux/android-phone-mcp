# Android Phone MCP Configuration
# Copy this file to .env and fill in the values

# ADB Connection (Choose one method)
# Method 1: USB Debugging (Usually automatic via ADB)
# ADB_HOST=localhost
# ADB_PORT=5555

# Method 2: Wireless ADB (Requires manual connection)
# Step 1: Connect device via USB and run: adb tcpip 5555
# Step 2: Find device IP: adb shell ip addr show wlan0
# Step 3: Disconnect USB and connect wirelessly: adb connect <DEVICE_IP>:5555

# Device Serial (Optional - auto-detected if not set)
# ANDROID_SERIAL=localhost:5555

# Screenshot Settings
SCREENSHOT_DIR=.active_screenshots

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
